# Requirements for Glove Detection Pipeline
# Install with: pip install -r requirements.txt

ultralytics==8.2.0  # YOLOv8 for detection (includes PyTorch integration)
torch==2.4.0  # PyTorch (CPU version; for GPU, use torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121)
torchvision==0.19.0
opencv-python==4.10.0.84  # OpenCV for image I/O and annotation
pillow==10.4.0  # PIL for image loading (handles truncated errors)
numpy==2.1.0  # For array operations in validation
argparse  # Built-in, but ensures CLI handling
json  # Built-in
pathlib  # Built-in